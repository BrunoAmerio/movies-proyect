import styled from 'styled-components';

export const Container = styled.div`
	overflow: hidden;

	.cardContainer {
		display: flex;
		width: fit-content;
		align-items: center;
		gap: 40px;
		padding: 10px 0px;
		border-radius: 10px;
		cursor: grab;
	}
`;
